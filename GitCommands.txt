git add .
git commit -m "..." 
git push origin main 

git push -u origin topfc-0.9


--Upload to Silver and Merge in Main --
git add .
git commit -m "Puntos Para retransmisiones cambiado"
git push origin silver

git checkout main
git pull origin main
git merge silver
git push origin main

git checkout silver