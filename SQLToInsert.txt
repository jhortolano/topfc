ALTER TABLE public.extra_playoffs_matches ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Cualquiera puede ver los partidos"
ON public.extra_playoffs_matches
FOR SELECT
TO anon, authenticated
USING (true);


-- Cambia la configuración de la vista de brackets
ALTER VIEW public.v_extra_playoffs_bracket_dinamico 
SET (security_invoker = on);

-- Cambia la configuración de la vista de posiciones
ALTER VIEW public.v_posiciones_dinamicas 
SET (security_invoker = on);
